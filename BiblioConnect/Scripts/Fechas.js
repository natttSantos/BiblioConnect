
function addDays(date, days)
{
    var date
    // Date.now() gives the epoch date value (in milliseconds) of current date 
    return nextDate = new Date(Date.now() + days * 24 * 60 * 60 * 1000)
    console.log(nextDate)
}