
@{
    ViewBag.Title = "Detalles Libro";
}
@using BiblioConnect.Modelo


<head>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    @* Boostrap CSS *@
    <link href="~/Content/bootstrap.min.css" rel="stylesheet">
    @* CSS DE MAPAS *@
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="" />
    <link rel="stylesheet" type="text/css" href="~/CSS/Detalles.css" />
    <link rel="stylesheet" type="text/css" href="~/CSS/Mapa.css" />
    <link rel="stylesheet" href="~/CSS/Modal.css">

</head>

<body>
    <div class="container">
        <div class="row">
            <div class="row row-start">
                <div class="col-md-3">
                    <img id="FotoLibro" src="#" alt="Imagen">
                </div>
                <div class="col-md-6">
                    <h3 id="Titulo" class="titulo-libro"></h3>
                    <h6 id="Autor" class="autor"></h6>
                    <p id="descripcionLibro"></p>
                    <a id="toggleDescripcion" href="#" class="purple-link">Mostrar más</a>
                </div>
                <div class="col-md-3 align-right">
                    <div class="card">
                        <div class="card-body">
                            <p class="card-text" id="txtCard"></p>
                            <div class="button-container">
                                <button class="btn-prestamo btn-centered" id="btnTransaccion"></button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row ficha-tecnica">
                <h5 class="title">Ficha Técnica</h5>
                <div class="col-md-2">
                    <h6 class="info-title">Editorial</h6>
                    <h6 class="info-value" id="Editorial"></h6>
                </div>
                <div class="col-md-2">
                    <h6 class="info-title">Idioma</h6>
                    <h6 class="info-value" id="Idioma"></h6>
                </div>
                <div class="col-md-2">
                    <h6 class="info-title">Categoria</h6>
                    <h6 class="info-value" id="Categoria"></h6>
                </div>
                <div class="col-md-2">
                    <h6 class="info-title">ISBN</h6>
                    <h6 class="info-value"> 90642832379</h6>
                </div>
            </div>
            <div class="row">
                <div class="col-md-3">
                    <img id="FotoAutor" src="#" alt="Imagen">
                </div>
                <div class="col-md-9 infoAutor">
                    <h5 class="title">Información sobre el autor</h5>
                    <h6 id="Autor"></h6>
                    <p id="descripcionAutor" style="height: 80px; "></p>
                    <a id="toggleAutor" href="#" class="purple-link">Mostrar más</a>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <h5 class="title">Ubicación biblioteca</h5>
                    <h6 id="Biblioteca"></h6>
                    <div id="map"></div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-7 infoReseñas">
                    <h5 class="title">Reseñas de los lectores</h5>
                    <div id="infoReseñas">
                        <p id="media-reseñas"></p>
                        <p id="sin-reseñas"></p>
                        <p id="estrellas-media"></p>
                    </div>
                </div>
            </div>
            <div class="row">
                <button id="botonEscribirReseña" type="button" class="btn-reseña" data-toggle="modal" data-target="#modalReseña">
                    Escribir reseña
                </button>
            </div>
            <div class="row">
                <div class="col-md-10">
                    <p id="num-reseñas"></p>
                    <div id="reseñas-list" class="descripcion"></div>
                </div>
            </div>
        </div>
    </div>

    @* MODAL RESEÑA *@

    <div class="modal fade" id="modalReseña" tabindex="-1" aria-labelledby="registroModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable custom-modal">
            <div class="modal-content">
                <div class="modal-header naranjaHeader">
                    <h4 class="modal-title" id="registroModalLabel">Escribir reseña</h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close" id="cerrar"></button>
                </div>
                <div class="modal-body">
                    <div class="row justify-content-center mt-3 mb-4">
                        <div class="col-md-12 col-12">
                            <textarea class="form-control" id="descripcionReseña" placeholder="Descripción"></textarea>
                        </div>
                    </div>
                    <div class="row justify-content-center mt-3 mb-4 ">
                        <div class="col-md-12 col-12">
                            <label for="puntuacion">Puntuación:</label>
                            <div class="rating">
                                <input type="radio" id="star5" name="puntuacion" value="5" /><label for="star5" title="5 estrellas"></label>
                                <input type="radio" id="star4" name="puntuacion" value="4" /><label for="star4" title="4 estrellas"></label>
                                <input type="radio" id="star3" name="puntuacion" value="3" /><label for="star3" title="3 estrellas"></label>
                                <input type="radio" id="star2" name="puntuacion" value="2" /><label for="star2" title="2 estrellas"></label>
                                <input type="radio" id="star1" name="puntuacion" value="1" /><label for="star1" title="1 estrella"></label>
                            </div>
                        </div>
                    </div>
                    <div class="row  d-flex justify-content-center align-items-center">
                        <div class="col-md-4">
                            <div class="form-floating button">
                                <input type="submit" class="btn-registrarNaranja" onclick="EnviarReseña()" value="Enviar" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>


@* JQquery, Popper.js, Boostrap.js *@
<script src="~/Scripts/jquery-3.7.0.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.11.6/umd/popper.min.js"></script>
<script src="~/Scripts/bootstrap.js"></script>
@* Libreria alert *@
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="~/Scripts/sweetAlert.js"></script>
@* JS DE MAPAS *@
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin=""></script>
@* FECHAS *@
<script src="~/Scripts/Fechas.js"></script>
<script src="~/Scripts/API_mapa.js"></script>
<script src="~/Scripts/MostrarNotificaciones.js"></script>

<script>
    $(document).ready(function () {
            var oReserva;
            var duracion = 21;
            var IdLector = @(((Usuario)Session["Usuario"]).Id);
            var IdBiblioteca;
            var Titulo;

            $.ajax({
                url: "/Lector/ObtenerLibro",
                type: "GET",
                data: { id: @Model },
                success: function (data) {
                    var libro = data.data;
                    $("#FotoLibro").attr("src", libro.Foto);
                    $("#Titulo").text(libro.Titulo);
                    $("#Editorial").text(libro.Editorial);
                    $("#Autor").text(libro.Autor);
                    $("#Idioma").text(libro.Idioma);
                    IdBiblioteca = libro.idBiblioteca;
                    Titulo = libro.Titulo;

                     oReserva = {
                        idBiblioteca: IdBiblioteca,
                        idLector: IdLector,
                        idLibro: @Model,
                        Estado: "En espera",
                     };

                    getDescripcionLibro(Titulo)
                    getDescripcionAutor(libro.Autor);
                    mostrarMapaBiblioteca(libro);
                    tipoTransaccion(oReserva)
                    getCategoria(libro.idCategoria);
                    getReseñas();

                },
                error: function (error) {
                    console.log("Error al obtener los datos del libro:", error);
                }
            });

            $("#botonEscribirReseña").on("click", function () {
                $('#modalReseña').modal('show');
            });
            //Navigate to Perfil Biblioteca
            $("#btnBiblioteca").on("click", function () {
                if (IdBiblioteca) {
                    window.location.href = "/Biblioteca/Perfil?id=" + IdBiblioteca;
                }
            });
            // TRANSACCION
            $("#btnTransaccion").on("click", function () {
                var oPrestamo = {
                    Estado: true,
                    IdBiblioteca: IdBiblioteca,
                    IdLector: IdLector,
                    IdLibro: @Model,
                    Duracion: duracion,
                };
                var btnTransaccion = $("#btnTransaccion");

                //REALIZAR PRÉSTAMO
                if (btnTransaccion.text() == "Préstamo") {
                    alertConfirm('¿Estás seguro de realizar el préstamo?',
                        'Si aceptas se te asignará como el próximo lector').then(() => {
                            registroPendienteRecogida(oPrestamo);
                        })
                //REALIZAR RESERVA
                } else {
                    alertConfirm('¿Estás seguro de realizar una reserva?',
                        'Si aceptas se te añadirá a la lista de espera del ejemplar').then(() => {
                            registroReserva(oReserva);
                        })
                }
            });
        });

function EnviarReseña() {
    var oReseña = {
        Descripcion: $("#descripcionReseña").val(),
        Puntuacion: $("input[name='puntuacion']:checked").val(),
        idLibro: @Model,
        idLector: @(((Usuario)Session["Usuario"]).Id),
    }
    $.ajax({
        url: "/Lector/RegistrarReseña",
        type: "POST",
        data: oReseña,
        success: function (data) {
            alertSuccess('¡Enhorabuena!', 'Acabas de escribir una reseña.').then(() => { window.location.reload();})
        },
        error: function (error) {
            console.log("Error al obtener los datos del prestamo:", error);
            reject(error);
        }
    });
}
function getReseñas() {
    var idLibro = @Model;
    var numReseñas = 0;
    var sumaPuntuacion = 0; 
    $.ajax({
        url: "/Lector/ListarReseñasPorLibro",
        type: "GET",
        data: { id: idLibro },
        success: function (data) {
            $("#reseñas-list").empty(); 
            $.each(data.data, function (index, reseña) {
                mostrarInfoReseña(reseña);
                numReseñas++;
                sumaPuntuacion += reseña.Puntuacion; 
            });
            mostrarInfoTotalReseñas(numReseñas, sumaPuntuacion);
        },
        error: function (error) {
            console.log("Error al obtener las reseñas:", error);
        }
    });
}

    function mostrarInfoReseña(reseña) {
        getLector(reseña.idLector).then(function (lector) {
            var estrellasHTML = "";
            for (var i = 1; i <= 5; i++) {
                var icono = i <= reseña.Puntuacion ? "fas fa-star" : "far fa-star";
                estrellasHTML += '<i class="' + icono + '"></i>';
            }

            var vistaReseña =
                '<div class="reseña">' +
                '  <div class="lector">' +
                '    <img src="' + lector.Foto + '" ' +
                '    <span>' + lector.Nombre + '</span>' +
                '  </div>' +
                '  <div class="contenido">' +
                '    <div class="descripcion">' + reseña.Descripcion + '</div>' +
                '    <div class="puntuacion">' + estrellasHTML + '</div>' +
                '    <div class="fecha">' + getDate(reseña.Fecha) + '</div>' +
                '  </div>' +
                '</div>';

            $("#reseñas-list").append(vistaReseña);
            $("#botonEscribirReseña").show();
        });
    }
    function mostrarInfoTotalReseñas(numReseñas, sumaPuntuacion) {
        const media = sumaPuntuacion / numReseñas;
        const mediaRedondeada = Math.round(media * 10) / 10; // Redondear media
        const estrellas = generarEstrellas(mediaRedondeada);

        $("#num-reseñas").text("(" + numReseñas + " opiniones de lectores" + ")");
        $("#estrellas-media").html(estrellas);
        if (numReseñas > 0) {
            $("#media-reseñas").text(mediaRedondeada + "/5");
        } else {
            $("#sin-reseñas").text("Todavía no hay reseñas. Sé el primero en dar tu opinión sobre el libro");
        }
    }
    function generarEstrellas(media) {
        const numEstrellas = 5;
        const estrellaLlena = '<i class="fas fa-star"></i>';
        const estrellaVacia = '<i class="far fa-star"></i>';
        let estrellasHTML = '';

        for (let i = 0; i < numEstrellas; i++) {
            if (i < Math.floor(media)) {
                estrellasHTML += estrellaLlena;
            } else {
                estrellasHTML += estrellaVacia;
            }
        }

        return estrellasHTML;
    }
        function getPrestamo() {
            return new Promise((resolve, reject) => {
                $.ajax({
                    url: "/Lector/ObtenerPrestamo",
                    type: "GET",
                    data: { id: @Model },
                    success: function (data) {
                        var prestamo = data.data;
                        resolve(prestamo);
                    },
                    error: function (error) {
                        console.log("Error al obtener los datos del prestamo:", error);
                        reject(error);
                    }
                });
            });
        }

        function tipoTransaccion(oReserva) {
            $.ajax({
                url: "/Lector/ValidarTipoTransaccion",
                type: "GET",
                data: { id: @Model },
                success: function (resultado) {
                    var disponible = resultado.resultado;
                    var btnTransaccion = $("#btnTransaccion");
                    var txtCard = $('#txtCard');

                    if (disponible) {
                        txtCard.text("Disponible para préstamo. ¡Llévalo contigo!");
                        btnTransaccion.text("Préstamo");
                    } else {
                        txtCard.text("El ejemplar no está disponible, resérvalo y te avisaremos cuando lo esté.");
                        ValidarUsuarioRepetido(oReserva)
                        btnTransaccion.text("Reserva");
                    }
                },
                error: function (error) {
                    console.log("Error al obtener los datos del autor:", error);
                }
            });
        }

    function registroPendienteRecogida(prestamoPendienteRecogida) {
        console.log(prestamoPendienteRecogida)
            $.ajax({
                url: '/Lector/RegistrarPendienteRecogida',
                type: 'POST',
                data: prestamoPendienteRecogida,
                success: function (data) {
                    oNotificacion = {
                        idBiblioteca: prestamoPendienteRecogida.IdBiblioteca,
                        idLector: prestamoPendienteRecogida.IdLector,
                        idLibro: prestamoPendienteRecogida.IdLibro
                    }
                    enviarNotificacion(oNotificacion);
                    alertSuccess('¡Enhorabuena!', 'Acabas de realizar un préstamo con éxito. Ya puedes ir a recogerlo a la biblioteca.').then(() => {
                        location.reload(); //Recarga la página actual cuando se pulsa OK
                    })
                },
                error: function (error) {
                    console.log('Error al realizar el préstamo', error);
                },
            });
        }
        function registroReserva(oReserva) {
             $.ajax({
                url: '/Lector/RegistrarReserva',
                type: 'POST',
                data: oReserva,
                success: function (data) {
                    alertSuccess('¡Enhorabuena!', 'Acabas de realizar una reserva con éxito').then(() => {
                    })
                },
                error: function (error) {
                    console.log('Error al realizar el préstamo', error);
                },
            });
        }
        function ValidarUsuarioRepetido(oReserva) {
            $.ajax({
                url: "/Lector/ValidarUsuarioRepetido",
                type: "GET",
                data: oReserva,
                success: function (resultado) {
                    var usuarioRepetido = resultado.resultado;
                    if (usuarioRepetido) {
                        var btnTransaccion = $("#btnTransaccion");
                        btnTransaccion.prop("disabled", true);
                    }
                },
                error: function (error) {
                    console.log("Error al validar usuario repetido:", error);
                }
            });
        }
        function ValidarPrimerPrestamo(idLibro) {
            $.ajax({
                url: "/Lector/ValidarPrimerPrestamo",
                type: "GET",
                data: { id: idLibro },
                success: function (data) {
                    var resultado = resultado.resultado;
                    return resultado;
                },
                error: function (error) {
                    console.log("Error al validar si es el primer prestamo:", error);
                }
            });
        }
        function getCategoria(idCategoria) {
            $.ajax({
                url: "/Lector/ObtenerCategoria",
                type: "GET",
                data: { id: idCategoria },
                success: function (data) {
                    var categoria = data.data;
                    $("#Categoria").text(categoria.Nombre);
                },
                error: function (error) {
                    console.log("Error al obtener los datos de la Categoria:", error);
                }
            });
        }
        function getEditorial(idEditorial) {
            $.ajax({
                url: "/Lector/ObtenerEditorial",
                type: "GET",
                data: { id: idEditorial },
                success: function (data) {
                    var editorial = data.data;
                    $("#Editorial").text(editorial.Nombre);
                },
                error: function (error) {
                    console.log("Error al obtener los datos de la Editorial:", error);
                }
            });
        }
    async function getBiblioteca(idBiblioteca) {
        try {
            const data = await $.ajax({
                url: "/Lector/ObtenerBiblio",
                type: "GET",
                data: { id: idBiblioteca },
            });
            return data.data;
        } catch (error) {
            console.log("Error al obtener los datos de la Biblioteca:", error);
            throw error;
        }
    }
    async function mostrarMapaBiblioteca(libro) {
        try {
            const biblio = await getBiblioteca(libro.idBiblioteca);
            const coordinatesLibro = await getLatLong(biblio.Calle, biblio.Ciudad, biblio.Estado, biblio.Pais, biblio.CodPostal);
            const lat = coordinatesLibro[0];
            const lon = coordinatesLibro[1];
            const distanciaLector = await getDistance(lat, lon);
            var mensajePopUp = "Usted está a " + distanciaLector + " kilómetros del libro."
            mostrarMapa_withdistance(lat, lon, mensajePopUp);

            // Crear el enlace con el icono de biblioteca
            const bibliotecaContainer = $("#Biblioteca");
            const bibliotecaIcon = $("<i>").addClass("fas fa-book icono-biblioteca");
            const bibliotecaLink = $("<a>")
                .attr("href", "/Biblioteca/Perfil?id=" + libro.idBiblioteca)
                .text(biblio.Nombre)
                .prepend(bibliotecaIcon);
            bibliotecaContainer.empty().append(bibliotecaLink);
        } catch (error) {
            console.log("Error:", error);
        }
    }
    function getDistance(lat1, lon1) {
        return new Promise(function (resolve, reject) {
            //Coordinates Biblioteca
                    //Coordinates User
                    return getUserCurrentPosition()
                        .then(function (coordinatesUser) {
                            const lat2 = coordinatesUser[0];
                            const lon2 = coordinatesUser[1];
                            const distance = calculateDistance(lat1, lon1, lat2, lon2);
                            const roundedDistance = distance.toFixed(2);
                            resolve(roundedDistance);
                        });
        });
    }
    function getLector(idLector) {
        return new Promise((resolve, reject) => {
        $.ajax({
            url: "/Lector/ObtenerLector",
            type: "GET",
            data: { id: idLector },
            success: function (data) {
                var lector = data.data;
                resolve(lector);
            },
            error: function (error) {
                console.log("Error al obtener los datos del lector:", error);
            }
        });
        });
    }
    function getDescripcionLibro(title) {
            $.ajax({
                url: "https://es.wikipedia.org/w/api.php",
                dataType: "jsonp",
                data: {
                    action: "query",
                    format: "json",
                    prop: "extracts",
                    exintro: true,
                    explaintext: true,
                    titles: title
                },
                success: function (response) {
                    var pages = response.query.pages;
                    var pageId = Object.keys(pages)[0];
                    var extract = pages[pageId].extract;
                    var maxWords = 100;
                    var fullContent = "";
                    if (extract) {
                        var cleanExtract = extract.replace(/[\[\(].*?[\]\)]/g, "");
                        $("#descripcionLibro").text(cleanExtract);
                        fullContent = cleanExtract;

                        var words = cleanExtract.split(" ");
                        if (words.length > maxWords) {
                            var shortenedContent = words.slice(0, maxWords).join(" ");
                            $("#descripcionLibro").text(shortenedContent + "...");
                            $("#toggleDescripcion").show();
                        } else {
                            $("#toggleDescripcion").hide();
                        }
                    } 
                    $("#toggleDescripcion").click(function (event) {
                        event.preventDefault();
                        var currentContent = $("#descripcionLibro").text();

                        if (currentContent === fullContent) {
                            var shortenedContent = words.slice(0, maxWords).join(" ");
                            $("#descripcionLibro").text(shortenedContent + "...");
                            $("#toggleDescripcion").text("Mostrar más");
                        } else {
                            $("#descripcionLibro").text(fullContent);
                            $("#toggleDescripcion").text("Mostrar menos");
                        }
                    });
                }
            });
    }
    function getDescripcionAutor(autor) {
        const url = "https://es.wikipedia.org/w/api.php";
        const maxWords = 50;
        let fullContent = "";
        let words = [];

        $.ajax({
            url: url,
            dataType: "jsonp",
            data: {
                action: "query",
                format: "json",
                prop: "extracts|pageimages",
                exintro: true,
                explaintext: true,
                titles: autor,
                piprop: "thumbnail",
                pithumbsize: 200
            },
            success: function (response) {
                const pages = response.query.pages;
                const pageId = Object.keys(pages)[0];
                const extract = pages[pageId].extract;
                const thumbnail = pages[pageId].thumbnail;

                if (extract) {
                    const cleanExtract = extract.replace(/[\[\(].*?[\]\)]/g, "");
                    $("#descripcionAutor").text(cleanExtract);
                    fullContent = cleanExtract;

                    words = cleanExtract.split(" ");
                    if (words.length > maxWords) {
                        const shortenedContent = words.slice(0, maxWords).join(" ");
                        $("#descripcionAutor").text(shortenedContent + "...");
                        $("#toggleAutor").show();
                    } else {
                        $("#toggleAutor").hide();
                    }
                }

                if (thumbnail) {
                    $("#FotoAutor").attr("src", thumbnail.source);
                    $("#FotoAutor").attr("alt", "Imagen del autor");
                }

                $("#toggleAutor").click(function (event) {
                    event.preventDefault();
                    const currentContent = $("#descripcionAutor").text();
                    if (currentContent === fullContent) {
                        const shortenedContent = words.slice(0, maxWords).join(" ");
                        $("#descripcionAutor").text(shortenedContent + "...");
                        $("#toggleAutor").text("Mostrar más");
                    } else {
                        $("#descripcionAutor").text(fullContent);
                        $("#toggleAutor").text("Mostrar menos");
                    }
                });
            }
        });
    }







</script>